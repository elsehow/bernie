{
  "name": "bernie",
  "author": "elsehow",
  "description": "redistribute values - equally, by default",
  "version": "0.0.1",
  "license": "BSD-3-Clause",
  "main": "src/index.js",
  "scripts": {
    "test": "tape test/*.js | tap-spec",
    "watch": "runwatch \"**/*.js\" -r \"npm test\""
  },
  "devDependencies": {
    "runwatch": "^0.1.3",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  },
  "dependencies": {
    "lodash.sum": "^4.0.2"
  }
}
